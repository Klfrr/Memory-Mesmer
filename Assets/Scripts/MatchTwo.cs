using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MatchTwo : MonoBehaviour
{

    int sizeX, sizeY, playerX, playerY;
    int[][] cardArray;
    int level;
    bool status;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void increaseX()
    {
        sizeX++;
    }

    void increaseY()
    {
        sizeY++;
    }

    void eachClick(GameObject click)
    {

    }
}
